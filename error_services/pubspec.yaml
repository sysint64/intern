name: error_services

environment:
  sdk: ">=2.15.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.3
  drivers:
    path: ../drivers

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  app_lints:
    path: ../app_lints

flutter:
